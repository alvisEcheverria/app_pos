const Roles = require('./roles.models');
const Users = require('./users.models');
const Clients = require('./clients.models');
const Categories = require('./categories.models');
const Products = require('./products.models');
const Orders = require('./orders.models');
const OrderDetails = require('./orderDetails.models');
const Invoice = require('./invoice.models');

module.exports = { 
    Roles,
    Users,
    Clients,
    Categories,
    Products,
    Orders,
    OrderDetails,
    Invoice
};